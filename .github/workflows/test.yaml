name: test

on:
  pull_request:
  branches: [master]
jobs:
  build:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v2
    - name: node setup
    - uses: actions/setup-node@v1
    - run: npm install &&  npm test
